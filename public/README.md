# public/

Static files served from the site root — a file at `public/assets/images/logo.svg`
is served at `/assets/images/logo.svg`.

```
public/
  assets/
    images/     logos, photography, any raster or vector artwork
```

Images and artwork go in `assets/images/`. See the README in that folder for
the filenames the site expects.
